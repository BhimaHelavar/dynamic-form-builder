// This file is no longer needed since we're not using SSR
// Keeping as an empty file to avoid breaking imports

import { ApplicationConfig } from '@angular/core';
import { appConfig } from './app.config';

export const config = appConfig;
